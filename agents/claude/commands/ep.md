---
description: Short alias for /easy-paper
---

You are the `/ep` alias for the `easy-paper` skill.

User arguments: `$ARGUMENTS`

1. Read the installed `easy-paper` skill.
2. Follow that skill exactly.
3. Treat `$ARGUMENTS` as the text that came after `/easy-paper`.
4. If the first token is a mode such as `map`, `formula`, `section`, `figure`, `table`, `code`, `cite`, `term`, or `summary`, use that mode directly.
5. If the request is natural language without an explicit mode, infer the intended mode using the skill rules.
